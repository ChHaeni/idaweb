% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search-data.r
\name{search_by_parameter}
\alias{search_by_parameter}
\title{Filter Metadata by Parameter}
\usage{
search_by_parameter(
  shortname,
  unit,
  group,
  description,
  granularity = c("T", "H", "D", "M", "Y"),
  language = c("en", "de", "fr", "it"),
  meta_data = idaweb::metadata,
  drop_nodata = FALSE
)
}
\arguments{
\item{shortname}{Parameter short name(s) to grep for.}

\item{unit}{Parameter unit(s) to grep for.}

\item{group}{Parameter group(s) to fuzzy-match (e.g. \code{"wind"},
\code{"temperature"}). Language-dependent.}

\item{description}{Parameter description(s) to fuzzy-match.
Language-dependent. Prefix with \code{=} for non-fuzzy (partial) matching.}

\item{granularity}{Temporal granularity(s): \code{"T"}, \code{"H"},
\code{"D"}, \code{"M"}, \code{"Y"}. Default is all.}

\item{language}{Language for \code{group} and \code{description} labels:
\code{"en"} (default), \code{"de"}, \code{"fr"}, or \code{"it"}.}

\item{meta_data}{A \code{met_metadata} object or list thereof. Defaults to
\code{\link[idaweb]{metadata}}.}

\item{drop_nodata}{Logical. If \code{TRUE}, collections with an empty
inventory after filtering are removed.}
}
\value{
A \code{met_metadata} object or a list thereof.
}
\description{
Subsets a \code{met_metadata} object (or list) based on measurement
parameter characteristics.
}
\examples{
\dontrun{
meta <- search_by_parameter(group = "precipitation", granularity = "D")
meta <- search_by_parameter(shortname = "tre200s0")
}
}
\seealso{
\code{\link{search_by_datetime}}, \code{\link{search_by_location}}
}
